// with if-else only

// const marks = 50;

// const results = marks < 35 ? "Your failed" : "You Cleared this !!"
// console.log(results);


// with multiple if else cases.
const marks = 500;

const results = marks < 35 ? "Fail" : marks < 40 ? "Pass" : marks < 60 ? "B Grade" : marks < 75 ? "A Grade" : marks < 90 ? "A+ Grade" : "Genius Siddu Baby";
console.log(results);